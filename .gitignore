# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
build/
dist/
*.egg
wheels/
share/python-wheels/
MANIFEST

# Virtualenv
.venv/
venv/
env/
ENV/

# uv
.uv-cache/

# Test / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
coverage.xml
*.cover
.hypothesis/

# Type / lint caches
.mypy_cache/
.ruff_cache/
.dmypy.json

# Env / secrets
.env
.env.local
.env.*.local

# Databases / data artifacts
*.db
*.sqlite
*.sqlite3
.loregraph-cache/

# data/ — keep this rule fine-grained.
# - data/books/   = raw source texts (copyrighted, never commit)
# - data/exports/ = derived JSON (committed; this is what the frontend loads)
# - data/cache/   = LLM intermediate state (transient)
data/books/
data/cache/
data/*.tmp
.goal/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Frontend (web UI) — node_modules + build artifacts. Source is tracked.
node_modules/
src/loregraph/web/static/
src/loregraph/web/frontend/dist/
src/loregraph/web/frontend/.vite/
src/loregraph/web/frontend/*.local

# OS
.DS_Store
Thumbs.db

# Logs / runtime
*.log

# Private reference docs (pencil .pen files, etc.)
*.pen
